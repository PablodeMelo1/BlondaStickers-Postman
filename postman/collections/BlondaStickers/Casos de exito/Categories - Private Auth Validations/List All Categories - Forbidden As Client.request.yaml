$kind: http-request
url: "{{baseUrl}}/api/categories"
method: GET
headers:
  Cookie: "{{clientCookie}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test('client role is forbidden', function () {
        pm.response.to.have.status(403);
      });
    language: text/javascript
order: 2000
