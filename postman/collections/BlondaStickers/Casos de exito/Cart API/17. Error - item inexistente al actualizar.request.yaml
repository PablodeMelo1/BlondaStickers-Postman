$kind: http-request
url: "{{base_url}}/api/cart/items/507f1f77bcf86cd799439011"
method: PUT
headers:
  Content-Type: application/json
  Cookie: "{{clientCookie}}"
body:
  type: text
  content: |-
    {
      "quantity": 1
    }
scripts:
  - type: afterResponse
    code: |-
      pm.test('item inexistente retorna 404', function () {
        pm.response.to.have.status(404);
      });
    language: text/javascript
order: 17000
