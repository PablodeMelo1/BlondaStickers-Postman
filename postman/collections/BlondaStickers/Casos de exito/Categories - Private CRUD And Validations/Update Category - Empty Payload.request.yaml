$kind: http-request
url: "{{baseUrl}}/api/categories/{{categoryId}}"
method: PATCH
headers:
  Content-Type: application/json
  Cookie: "{{adminCookie}}"
body:
  type: text
  content: "{}"
scripts:
  - type: afterResponse
    code: |-
      pm.test('empty update returns 400', function () {
        pm.response.to.have.status(400);
      });
    language: text/javascript
order: 9000
