$kind: http-request
url: "{{baseUrl}}/api/categories/invalid-id/visibility"
method: PATCH
headers:
  Cookie: "{{adminCookie}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test('invalid id returns 400', function () {
        pm.response.to.have.status(400);
      });
    language: text/javascript
order: 13000
